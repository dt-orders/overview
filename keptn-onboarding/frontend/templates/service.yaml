--- 
apiVersion: v1
kind: Service
metadata: 
  name: front-end
spec:
  type: ClusterIP
  ports:
  - name: http
    port: {{ .Values.service.externalPort }}
    targetPort: {{ .Values.service.internalPort }}
    protocol: TCP
  selector: 
    app: front-end
